import { useAuth } from "./context/AuthContext";
import { supabase } from "../supabaseClient";

const StudentTest = () => {


    return (
      <div>
        <p className="text-black">This is page for students</p>
      </div>
    );
}

export default StudentTest